   //BufferedReader reader;
    //String line;
    //reader = createReader("path.txt");

    //try {
    //  line = reader.readLine();
    //}
    //catch (IOException e) {
    //  e.printStackTrace();
    //  line = null;
    //}
    //String movieFullPath = line;


    //PrintWriter output;
    //output = createWriter("path.txt");
    //output.println(movieFullPath);
    //output.flush(); // Writes the remaining data to the file
    //output.close(); // Finishes the file